export default class GalleryViewItemVO {
  constructor (index, name, uri, imageUrl, width, height) {
    this.index = index
    this.name = name
    this.uri = uri
    this.imageUrl = imageUrl
    this.width = width
    this.height = height
  }
}
